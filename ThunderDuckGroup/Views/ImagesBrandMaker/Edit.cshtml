@model IQueryable<ThunderDuckGroup.Models.Td_Images>
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutWebmaster.cshtml";
    string[] arrStr;
}

<div class="main-wrapper">
    <div class="container">

        <!-- Start breadcrumb section  -->
        <div class="row">
            <div class="col-lg-12">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/webmaster/index">Trang chủ</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Chỉnh sửa hình ảnh</li>
                    </ol>
                </nav>
            </div>
        </div>

        <!-- Start form -->
        <div class="form-area">
            <div class="row">

                <div class="col-lg-12">
                    <div class="single-form">
                        <div class="section-head">
                            <div class="title">
                                <h4>Chỉnh sửa hình ảnh</h4>
                            </div>
                        </div>

                        <div class="section-body">
                            <form action="/imagesbrandmaker/edit" method="post" enctype="multipart/form-data">
                                <div class="form-group">
                                    <label for="exampleFormControlInput4">Tiêu đề (VUI LÒNG NHẬP DƯỚI 150 KÍ TỰ)</label>
                                    <input type="text" class="form-control" placeholder="" name="title" value="@Model.FirstOrDefault().Title">
                                </div>
                                <div class="row">
                                    <div class="col-lg-3">
                                        @if (Model.FirstOrDefault().images1 != null)
                                        {
                                            if (Model.FirstOrDefault().images1.Contains(","))
                                            {
                                                arrStr = Model.FirstOrDefault().images1.Split(',');
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 1 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images1" accept="image/*" multiple="multiple" />
                                                    <div class="row">
                                                        @foreach (var img in arrStr)
                                                        {

                                                            <div class="col-sm-3 bg-slider" style="background: url('../../Images/ThunderDuckGroup/imageHome/@img') center center no-repeat / cover;">
                                                            </div>
                                                        }
                                                    </div>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 1 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images1" accept="image/*" multiple="multiple" />
                                                    <img src="~/Images/ThunderDuckGroup/imageHome/@Model.FirstOrDefault().images1" style="width:100%" />
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                <label for="exampleFormControlFile1">Hình ảnh 1 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                <br />
                                                <input type="file" name="images1" accept="image/*" multiple="multiple" />
                                            </div>
                                        }
                                    </div>

                                    <div class="col-lg-3">
                                        @if (Model.FirstOrDefault().images2 != null)
                                        {
                                            if (Model.FirstOrDefault().images2.Contains(","))
                                            {
                                                arrStr = Model.FirstOrDefault().images2.Split(',');
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 2 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images2" accept="image/*" multiple="multiple" />
                                                    <div class="row">
                                                        @foreach (var img in arrStr)
                                                        {

                                                            <div class="col-sm-3 bg-slider" style="background: url('../../Images/ThunderDuckGroup/imageHome/@img') center center no-repeat / cover;">
                                                            </div>
                                                        }
                                                    </div>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 2 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images2" accept="image/*" multiple="multiple" />
                                                    <img src="~/Images/ThunderDuckGroup/imageHome/@Model.FirstOrDefault().images2" style="width:100%" />
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                <label for="exampleFormControlFile1">Hình ảnh 2 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                <br />
                                                <input type="file" name="images2" accept="image/*" multiple="multiple" />
                                            </div>
                                        }
                                    </div>

                                    <div class="col-lg-3">
                                        @if (Model.FirstOrDefault().images3 != null)
                                        {
                                            if (Model.FirstOrDefault().images3.Contains(","))
                                            {
                                                arrStr = Model.FirstOrDefault().images3.Split(',');
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 3 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images3" accept="image/*" multiple="multiple" />
                                                    <div class="row">
                                                        @foreach (var img in arrStr)
                                                        {

                                                            <div class="col-sm-3 bg-slider" style="background: url('../../Images/ThunderDuckGroup/imageHome/@img') center center no-repeat / cover;">
                                                            </div>
                                                        }
                                                    </div>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 3 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images3" accept="image/*" multiple="multiple" />
                                                    <img src="~/Images/ThunderDuckGroup/imageHome/@Model.FirstOrDefault().images3" style="width:100%" />
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                <label for="exampleFormControlFile1">Hình ảnh 3 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                <br />
                                                <input type="file" name="images3" accept="image/*" multiple="multiple" />
                                            </div>
                                        }
                                    </div>

                                    <div class="col-lg-3">
                                        @if (Model.FirstOrDefault().images4 != null)
                                        {
                                            if (Model.FirstOrDefault().images4.Contains(","))
                                            {
                                                arrStr = Model.FirstOrDefault().images4.Split(',');
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 4 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images4" accept="image/*" multiple="multiple" />
                                                    <div class="row">
                                                        @foreach (var img in arrStr)
                                                        {

                                                            <div class="col-sm-3 bg-slider" style="background: url('../../Images/ThunderDuckGroup/imageHome/@img') center center no-repeat / cover;">
                                                            </div>
                                                        }
                                                    </div>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 4 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images4" accept="image/*" multiple="multiple" />
                                                    <img src="~/Images/ThunderDuckGroup/imageHome/@Model.FirstOrDefault().images4" style="width:100%" />
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                <label for="exampleFormControlFile1">Hình ảnh 4 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                <br />
                                                <input type="file" name="images4" accept="image/*" multiple="multiple" />
                                            </div>
                                        }
                                    </div>

                                    <div class="col-lg-3">
                                        @if (Model.FirstOrDefault().images5 != null)
                                        {
                                            if (Model.FirstOrDefault().images5.Contains(","))
                                            {
                                                arrStr = Model.FirstOrDefault().images5.Split(',');
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 5 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images5" accept="image/*" multiple="multiple" />
                                                    <div class="row">
                                                        @foreach (var img in arrStr)
                                                        {

                                                            <div class="col-sm-3 bg-slider" style="background: url('../../Images/ThunderDuckGroup/imageHome/@img') center center no-repeat / cover;">
                                                            </div>
                                                        }
                                                    </div>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 5 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images5" accept="image/*" multiple="multiple" />
                                                    <img src="~/Images/ThunderDuckGroup/imageHome/@Model.FirstOrDefault().images5" style="width:100%" />
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                <label for="exampleFormControlFile1">Hình ảnh 5 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                <br />
                                                <input type="file" name="images5" accept="image/*" multiple="multiple" />
                                            </div>
                                        }
                                    </div>

                                    <div class="col-lg-3">
                                        @if (Model.FirstOrDefault().images6 != null)
                                        {
                                            if (Model.FirstOrDefault().images6.Contains(","))
                                            {
                                                arrStr = Model.FirstOrDefault().images6.Split(',');
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 6 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images6" accept="image/*" multiple="multiple" />
                                                    <div class="row">
                                                        @foreach (var img in arrStr)
                                                        {

                                                            <div class="col-sm-3 bg-slider" style="background: url('../../Images/ThunderDuckGroup/imageHome/@img') center center no-repeat / cover;">
                                                            </div>
                                                        }
                                                    </div>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 6 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images6" accept="image/*" multiple="multiple" />
                                                    <img src="~/Images/ThunderDuckGroup/imageHome/@Model.FirstOrDefault().images6" style="width:100%" />
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                <label for="exampleFormControlFile1">Hình ảnh 6 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                <br />
                                                <input type="file" name="images6" accept="image/*" multiple="multiple" />
                                            </div>
                                        }
                                    </div>

                                    <div class="col-lg-3">
                                        @if (Model.FirstOrDefault().images7 != null)
                                        {
                                            if (Model.FirstOrDefault().images7.Contains(","))
                                            {
                                                arrStr = Model.FirstOrDefault().images1.Split(',');
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 7 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images7" accept="image/*" multiple="multiple" />
                                                    <div class="row">
                                                        @foreach (var img in arrStr)
                                                        {

                                                            <div class="col-sm-3 bg-slider" style="background: url('../../Images/ThunderDuckGroup/imageHome/@img') center center no-repeat / cover;">
                                                            </div>
                                                        }
                                                    </div>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 7 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images7" accept="image/*" multiple="multiple" />
                                                    <img src="~/Images/ThunderDuckGroup/imageHome/@Model.FirstOrDefault().images7" style="width:100%" />
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                <label for="exampleFormControlFile1">Hình ảnh 7 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                <br />
                                                <input type="file" name="images7" accept="image/*" multiple="multiple" />
                                            </div>
                                        }
                                    </div>

                                    <div class="col-lg-3">
                                        @if (Model.FirstOrDefault().images8 != null)
                                        {
                                            if (Model.FirstOrDefault().images8.Contains(","))
                                            {
                                                arrStr = Model.FirstOrDefault().images8.Split(',');
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 8 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images8" accept="image/*" multiple="multiple" />
                                                    <div class="row">
                                                        @foreach (var img in arrStr)
                                                        {

                                                            <div class="col-sm-3 bg-slider" style="background: url('../../Images/ThunderDuckGroup/imageHome/@img') center center no-repeat / cover;">
                                                            </div>
                                                        }
                                                    </div>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="form-group">
                                                    <label for="exampleFormControlFile1">Hình ảnh 8 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                    <br />
                                                    <input type="file" name="images8" accept="image/*" multiple="multiple" />
                                                    <img src="~/Images/ThunderDuckGroup/imageHome/@Model.FirstOrDefault().images8" style="width:100%" />
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="form-group">
                                                <label for="exampleFormControlFile1">Hình ảnh 8 (VUI LÒNG THÊM HÌNH VỚI KÍCH CỠ 1920x720 HOẶC LỚN HƠN)</label>
                                                <br />
                                                <input type="file" name="images8" accept="image/*" multiple="multiple" />
                                            </div>
                                        }
                                    </div>
                                </div>
                                <input class="btn btn-primaryt" type="submit" value="Lưu" />
                            </form>
                        </div>

                    </div>
                </div>


            </div>
        </div>
    </div>
</div>
<script src="~/Content/Webmaster/js/ckeditor/ckeditor.js"></script>
<script src="~/Content/Webmaster/js/ckeditor/samples/js/sample.js"></script>